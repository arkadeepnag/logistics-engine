// src/modules/shipments/constants/shipment-status.ts
export enum ShipmentStatus {
  CREATED = 'created',
  IN_TRANSIT = 'in_transit',
  OUT_FOR_DELIVERY = 'out_for_delivery',
  DELIVERED = 'delivered',
  RETURN = 'return',
  CANCELLED = 'cancelled',
}

